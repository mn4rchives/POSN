#include <bits/stdc++.h>
using namespace std;
#define int long long
#define debug(x) cerr<<#x<<"="<<x<<"\n"

int32_t main(){
    cin.tie(0);
    ios::sync_with_stdio(0);

    string s1, s2;
    cin>>s1>>s2;

    vector<int>v(s1.size());
    int count=0;
    for(char c1:s1){
        for(char c2:s2){
            if(c1==c2){
                
            }

        }
    }

    return 0;  
}